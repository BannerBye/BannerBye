{
  "name": "bannerbye-phase2b-analyzer",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Phase 2B — headless analyse van gemelde sites + keyword auto-fix voorstellen.",
  "scripts": {
    "analyze": "node --experimental-strip-types analyze.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
